version: "3"

#services:
#  server:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    tty: true
#    stdin_open: true
#    volumes:
#      - .:/app


services:
  server:
    image: php:8.1-apache
    ports:
      - "80:80"
    volumes:
      - .:/var/www/html

