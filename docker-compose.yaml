version: "3"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    volumes:
      - .:/app


#services:
#  server:
#    image: php:8.1-apache
#    ports:
#      - "80:80"
#    tty: true
#    stdin_open: true
#    volumes:
#      - .:/var/www/html

  composer:
    image: composer/composer
    command: composer install
    volumes:
      - ./:/app
